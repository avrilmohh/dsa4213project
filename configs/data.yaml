# Edit paths or dataset names as needed
dataset_imdb: "imdb"
dataset_rotten_tomatoes: "rotten_tomatoes"

# Split ratios
val_size: 0.10
test_size: 0.10

# Reproducibility
seed: 42

# For quick debugging runs, keep null
max_samples: null

# Output locations relative to repo root
output_tables: "results/tables"
output_figures: "results/figures"
output_splits: "data/splits"